-- A map script will run the returned function on the current map
-- Map scripts can be selected and run from the "Tools->Run Script" menu in the map editor

return function(map)
   -- Write your map script here
end
